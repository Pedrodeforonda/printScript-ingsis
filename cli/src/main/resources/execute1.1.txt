let a: string = 12 + " hola";
let b: string = readInput("Escribe tu Nombre: ");
const c: boolean = true;
if ( c ) {
    println("Hello " + a);
} else {
    println(readEnv("var"));
}

println(b + "Es un puto");