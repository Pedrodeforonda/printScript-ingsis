package main

enum class TokenType {
    LET_KEYWORD,
    CALL_FUNC,
    STRING_TYPE,
    LEFT_PAREN,
    RIGHT_PAREN,
    COMMA,
    NUMBER_TYPE,
    TYPE_ASSIGNATION,
    IDENTIFIER,
    ASSIGNATION,
    STRING_LITERAL,
    NUMBER_LITERAL,
    SEMICOLON,
    PLUS,
    MINUS,
    SLASH,
    ASTERISK,
    LEFT_BRACE,
    RIGHT_BRACE,
    IF_KEYWORD,
    ELSE_KEYWORD,
    BOOLEAN_LITERAL,
    BOOLEAN_TYPE,
    CONST_KEYWORD,
}
