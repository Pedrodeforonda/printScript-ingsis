package main

enum class TokenType {
    LET_KEYWORD,
    CALL_FUNC,
    STRING_TYPE,
    BOOLEAN_TYPE,
    LEFT_PAREN,
    RIGHT_PAREN,
    COMMA,
    NUMBER_TYPE,
    TYPE_ASSIGNATION,
    IDENTIFIER,
    ASSIGNATION,
    STRING_LITERAL,
    NUMBER_LITERAL,
    BOOLEAN_LITERAL,
    SEMICOLON,
    PLUS,
    MINUS,
    SLASH,
    ASTERISK,
    LEFT_BRACE,
    RIGHT_BRACE,
    IF_KEYWORD,
    ELSE_KEYWORD,
    CONST_KEYWORD,
    READ_ENV,
    READ_INPUT,
}
