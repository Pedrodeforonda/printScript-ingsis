enum class TokenType {
    LET_KEYWORD,
    CALL_FUNC,
    STRING_TYPE,
    NUMBER_TYPE,
    TYPE_ASSIGNATION,
    IDENTIFIER,
    ASSIGNATION,
    STRING_LITERAL,
    NUMBER_LITERAL,
    SEMICOLON,
    OPERATOR,
    COMPARE_OPERATOR,
}